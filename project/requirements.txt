# Core — all of these are genuinely required, not optional.
# (Week 9 accuracy pass: seaborn and pytest were previously commented out
# as "add if you need them", but visualisations.py hard-imports seaborn,
# and the documented `pytest tests/ -v` command needs pytest installed.
# `pip install -r requirements.txt` alone used to leave both missing.)
pandas
numpy
matplotlib
seaborn
scikit-learn
pytest
pytest-cov